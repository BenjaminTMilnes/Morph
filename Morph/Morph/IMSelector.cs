using System;
using System.Collections.Generic;
using System.Text;

namespace Morph
{
    /// <summary>
    /// An interface for Morph selectors. Selectors are essentially instructions for how to filter the list of elements in a document, to determine which ones a set of style rules should be applied to. These work in a very similar way to CSS selectors.
    /// </summary>
    public interface IMSelector
    {
    }
}
